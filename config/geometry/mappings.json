{
  "mappings": [
    {
      "find": "\\\\frac\\{([^{}]+)\\}\\{([^{}]+)\\}",
      "replace": "\\1a\\2",
      "type": "regex",
      "description": "Phân số \\frac{x}{y} → xay"
    },
    {
      "find": "\\\\sqrt\\{([^}]+)\\}",
      "replace": "s\\1)",
      "type": "regex",
      "description": "\\sqrt{x} → sx)"
    },
    {
      "find": "sqrt\\{([^}]+)\\}",
      "replace": "s\\1)",
      "type": "regex",
      "description": "sqrt{x} → sx)"
    },
    {
      "find": "\\\\sqrt\\(([^)]+)\\)",
      "replace": "s\\1)",
      "type": "regex",
      "description": "\\sqrt(x) → sx)"
    },
    {
      "find": "sqrt\\(([^)]+)\\)",
      "replace": "s\\1)",
      "type": "regex",
      "description": "sqrt(x) → sx)"
    },
    {
      "find": "\\\\sin\\(",
      "replace": "j(",
      "type": "regex",
      "description": "\\sin( → j("
    },
    {
      "find": "sin\\(",
      "replace": "j(",
      "type": "regex",
      "description": "sin( → j("
    },
    {
      "find": "\\\\cos\\(",
      "replace": "k(",
      "type": "regex",
      "description": "\\cos( → k("
    },
    {
      "find": "cos\\(",
      "replace": "k(",
      "type": "regex",
      "description": "cos( → k("
    },
    {
      "find": "\\\\tan\\(",
      "replace": "l(",
      "type": "regex",
      "description": "\\tan( → l("
    },
    {
      "find": "tan\\(",
      "replace": "l(",
      "type": "regex",
      "description": "tan( → l("
    },
    {
      "find": "\\\\ln\\(",
      "replace": "h(",
      "type": "regex",
      "description": "\\ln( → h("
    },
    {
      "find": "ln\\(",
      "replace": "h(",
      "type": "regex",
      "description": "ln( → h("
    },

    {
      "find": "\\}",
      "replace": ")",
      "type": "regex",
      "description": "} → )"
    },
    {
      "find": "\\{",
      "replace": "(",
      "type": "regex",
      "description": "{ → ("
    },
    {
      "find": "-",
      "replace": "p",
      "type": "literal",
      "description": "- → p"
    },
    {
      "find": "\\*",
      "replace": "O",
      "type": "regex",
      "description": "* → O"
    },
    {
      "find": "\\/",
      "replace": "P",
      "type": "regex",
      "description": "/ → P"
    }
  ]
}
